import type { NearbySearchFilters, PlaceCard } from '../../../../packages/shared-types/src';

export async function searchNearbyPlaces(_filters: NearbySearchFilters): Promise<PlaceCard[]> {
  // Placeholder para chamada real ao backend.
  return [];
}
